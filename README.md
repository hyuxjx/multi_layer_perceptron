# multi_layer_perceptron
A simple Multi-Layer Perceptron (MLP) implemented from scratch using NumPy to solve the XOR classification problem. Both forward propagation and backpropagation are manually coded without using deep learning frameworks.
